services:
  backend-api:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env  
    environment:
    - PYTHONPATH=/app/app

  frontend-ui:
    build: ./frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend-api